// ExpenseIncomeEntry.java
// Plain data object (POJO).
// Holds date (String), description (String), amount (double) and type (String).
// Has getters/setters and a constructor.

package expense_income_tracker;

public class Expense_Income_Tracker {
    public static void main(String[] args) {
        new ExpensesIncomesTracker().setLocationRelativeTo(null);
    }
}